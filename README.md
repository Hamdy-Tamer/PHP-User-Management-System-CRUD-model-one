# PHP-User-Management-System-CRUD
A complete PHP/MySQL CRUD application for managing user records with server-side validation. Features include add, view, update, delete operations, duplicate phone/email checking, Bootstrap 5 UI, Font Awesome icons, and session-based error handling with input persistence. Perfect for learning basic PHP CRUD operations.
